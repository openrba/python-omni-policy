vault_aws_auth_backend_identity_whitelist:
    status: ASSESS
    layout: vault
    page_title: 'Vault: vault_aws_auth_backend_identity_whitelist resource'
    sidebar_current: docs-vault-resource-aws-auth-backend-identity-whitelist
    description: Configures the periodic tidying operation of the whitelisted identity
        entries.
    arguments:
        backend:
            description: The path of the AWS backend being configured.
            required: false
            policy: ''
            notes: ''
        safety_buffer:
            description: The amount of extra time, in minutes, that must
            required: false
            policy: ''
            notes: ''
        disable_periodic_tidy:
            description: If set to true, disables the periodic
            required: false
            policy: ''
            notes: ''
    attributes: {}
    timeouts: {}
