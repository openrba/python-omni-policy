aws_api_gateway_usage_plan_key:
    status: HOLD
    subcategory: API Gateway (REST APIs)
    layout: aws
    page_title: 'AWS: aws_api_gateway_usage_plan_key'
    description: Provides an API Gateway Usage Plan Key.
    arguments:
        key_id:
            description: The identifier of the API key resource.
            required: true
            policy: ''
            notes: ''
        key_type:
            description: The type of the API key resource. Currently, the valid key
                type is API_KEY.
            required: true
            policy: ''
            notes: ''
        usage_plan_id:
            description: The Id of the usage plan resource representing to associate
                the key to.
            required: true
            policy: ''
            notes: ''
    attributes:
        id:
            description: The Id of a usage plan key.
        key_id:
            description: The identifier of the API gateway key resource.
        key_type:
            description: The type of a usage plan key. Currently, the valid key type
                is API_KEY.
        usage_plan_id:
            description: The ID of the API resource
        name:
            description: The name of a usage plan key.
        value:
            description: The value of a usage plan key.
    timeouts: {}
