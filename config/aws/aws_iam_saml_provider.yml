aws_iam_saml_provider:
    status: HOLD
    subcategory: IAM
    layout: aws
    page_title: 'AWS: aws_iam_saml_provider'
    description: Provides an IAM SAML provider.
    arguments:
        name:
            description: The name of the provider to create.
            required: true
            policy: ''
            notes: ''
        saml_metadata_document:
            description: An XML document generated by an identity provider that supports
                SAML 2.0.
            required: true
            policy: ''
            notes: ''
    attributes:
        arn:
            description: The ARN assigned by AWS for this provider.
        valid_until:
            description: The expiration date and time for the SAML provider in RFC1123
                format, e.g. `Mon, 02 Jan 2006 15:04:05 MST`.
    timeouts: {}
