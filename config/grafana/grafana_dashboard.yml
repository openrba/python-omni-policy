grafana_dashboard:
    status: ASSESS
    layout: grafana
    page_title: 'Grafana: grafana_dashboard'
    sidebar_current: docs-grafana-resource-dashboard
    description: The grafana_dashboard resource allows a Grafana dashboard to be created.
    arguments:
        config_json:
            description: The JSON configuration for the dashboard.
            required: true
            policy: ''
            notes: ''
        folder:
            description: The internal id of the folder
            required: false
            policy: ''
            notes: ''
    attributes:
        slug:
            description: A URL "slug" for this dashboard, generated by Grafana by
                removing
    timeouts: {}
